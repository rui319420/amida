export type VerticalLine = {
  id: string;
  lineIndex: number;
  x: number;
}

export type HorizontalLine = {
  id: string;
  leftLineIndex: number;
  y: number;
}

export type Point = {
  x: number;
  y: number;
}