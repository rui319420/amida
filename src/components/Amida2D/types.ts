export type VerticalLine = {
  id: string;
  lineIndex: number;
  x: number;
  name: string;
}

export type HorizontalLine = {
  id: string;
  index1: number;
  index2: number;
  y: number;
}

export type Point = {
  x: number;
  y: number;
}